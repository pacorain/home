url: |
  http://{{ states('input_text.itunes_host') }}/play_song
method: POST
payload: |
  {% set body = {'name': name } %}
  {{ body|to_json(ensure_ascii=True) }}
content_type: "json"
verify_ssl: false
